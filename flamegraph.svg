<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="422" onload="init(evt)" viewBox="0 0 1200 422" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="422" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="405.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="405.00"> </text><svg id="frames" x="10" width="1180" total_samples="1858"><g><title>hvmc`hvmc::run::Net::parallel_normal (1 samples, 0.05%)</title><rect x="0.0000%" y="277" width="0.0538%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="1"/><text x="0.2500%" y="287.50"></text></g><g><title>libsystem_malloc.dylib`szone_malloc_should_clear (1 samples, 0.05%)</title><rect x="0.0000%" y="261" width="0.0538%" height="15" fill="rgb(217,0,24)" fg:x="0" fg:w="1"/><text x="0.2500%" y="271.50"></text></g><g><title>libsystem_platform.dylib`__bzero (1 samples, 0.05%)</title><rect x="0.0000%" y="245" width="0.0538%" height="15" fill="rgb(221,193,54)" fg:x="0" fg:w="1"/><text x="0.2500%" y="255.50"></text></g><g><title>hvmc`main (12 samples, 0.65%)</title><rect x="0.0000%" y="341" width="0.6459%" height="15" fill="rgb(248,212,6)" fg:x="0" fg:w="12"/><text x="0.2500%" y="351.50"></text></g><g><title>hvmc`std::rt::lang_start (12 samples, 0.65%)</title><rect x="0.0000%" y="325" width="0.6459%" height="15" fill="rgb(208,68,35)" fg:x="0" fg:w="12"/><text x="0.2500%" y="335.50"></text></g><g><title>hvmc`std::sys_common::backtrace::__rust_begin_short_backtrace (12 samples, 0.65%)</title><rect x="0.0000%" y="309" width="0.6459%" height="15" fill="rgb(232,128,0)" fg:x="0" fg:w="12"/><text x="0.2500%" y="319.50"></text></g><g><title>hvmc`hvmc::main (12 samples, 0.65%)</title><rect x="0.0000%" y="293" width="0.6459%" height="15" fill="rgb(207,160,47)" fg:x="0" fg:w="12"/><text x="0.2500%" y="303.50"></text></g><g><title>libsystem_malloc.dylib`_szone_free (11 samples, 0.59%)</title><rect x="0.0538%" y="277" width="0.5920%" height="15" fill="rgb(228,23,34)" fg:x="1" fg:w="11"/><text x="0.3038%" y="287.50"></text></g><g><title>libsystem_malloc.dylib`free_large (11 samples, 0.59%)</title><rect x="0.0538%" y="261" width="0.5920%" height="15" fill="rgb(218,30,26)" fg:x="1" fg:w="11"/><text x="0.3038%" y="271.50"></text></g><g><title>libsystem_malloc.dylib`mvm_deallocate_pages (11 samples, 0.59%)</title><rect x="0.0538%" y="245" width="0.5920%" height="15" fill="rgb(220,122,19)" fg:x="1" fg:w="11"/><text x="0.3038%" y="255.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_deallocate_trap (11 samples, 0.59%)</title><rect x="0.0538%" y="229" width="0.5920%" height="15" fill="rgb(250,228,42)" fg:x="1" fg:w="11"/><text x="0.3038%" y="239.50"></text></g><g><title>dyld`start (13 samples, 0.70%)</title><rect x="0.0000%" y="357" width="0.6997%" height="15" fill="rgb(240,193,28)" fg:x="0" fg:w="13"/><text x="0.2500%" y="367.50"></text></g><g><title>libdyld.dylib`dyld4::LibSystemHelpers::getenv (1 samples, 0.05%)</title><rect x="0.6459%" y="341" width="0.0538%" height="15" fill="rgb(216,20,37)" fg:x="12" fg:w="1"/><text x="0.8959%" y="351.50"></text></g><g><title>libsystem_kernel.dylib`__exit (1 samples, 0.05%)</title><rect x="0.6459%" y="325" width="0.0538%" height="15" fill="rgb(206,188,39)" fg:x="12" fg:w="1"/><text x="0.8959%" y="335.50"></text></g><g><title>libsystem_kernel.dylib`__exit (1 samples, 0.05%)</title><rect x="0.6997%" y="357" width="0.0538%" height="15" fill="rgb(217,207,13)" fg:x="13" fg:w="1"/><text x="0.9497%" y="367.50"></text></g><g><title>libsystem_pthread.dylib`_pthread_start (1 samples, 0.05%)</title><rect x="0.7535%" y="357" width="0.0538%" height="15" fill="rgb(231,73,38)" fg:x="14" fg:w="1"/><text x="1.0035%" y="367.50"></text></g><g><title>hvmc`hvmc::run::Net::anni2 (18 samples, 0.97%)</title><rect x="0.8073%" y="293" width="0.9688%" height="15" fill="rgb(225,20,46)" fg:x="15" fg:w="18"/><text x="1.0573%" y="303.50"></text></g><g><title>hvmc`hvmc::run::Net::call (16 samples, 0.86%)</title><rect x="1.7761%" y="293" width="0.8611%" height="15" fill="rgb(210,31,41)" fg:x="33" fg:w="16"/><text x="2.0261%" y="303.50"></text></g><g><title>hvmc`hvmc::run::Net::comm02 (10 samples, 0.54%)</title><rect x="2.6372%" y="293" width="0.5382%" height="15" fill="rgb(221,200,47)" fg:x="49" fg:w="10"/><text x="2.8872%" y="303.50"></text></g><g><title>hvmc`hvmc::run::Net::comm22 (5 samples, 0.27%)</title><rect x="3.1755%" y="293" width="0.2691%" height="15" fill="rgb(226,26,5)" fg:x="59" fg:w="5"/><text x="3.4255%" y="303.50"></text></g><g><title>hvmc`hvmc::run::Net::half_free (44 samples, 2.37%)</title><rect x="3.4446%" y="293" width="2.3681%" height="15" fill="rgb(249,33,26)" fg:x="64" fg:w="44"/><text x="3.6946%" y="303.50">hv..</text></g><g><title>hvmc`hvmc::run::Net::alloc (213 samples, 11.46%)</title><rect x="17.9225%" y="277" width="11.4639%" height="15" fill="rgb(235,183,28)" fg:x="333" fg:w="213"/><text x="18.1725%" y="287.50">hvmc`hvmc::run::N..</text></g><g><title>hvmc`hvmc::run::Net::anni2 (140 samples, 7.53%)</title><rect x="29.3864%" y="277" width="7.5350%" height="15" fill="rgb(221,5,38)" fg:x="546" fg:w="140"/><text x="29.6364%" y="287.50">hvmc`hvmc:..</text></g><g><title>hvmc`hvmc::run::Net::call (137 samples, 7.37%)</title><rect x="36.9214%" y="277" width="7.3735%" height="15" fill="rgb(247,18,42)" fg:x="686" fg:w="137"/><text x="37.1714%" y="287.50">hvmc`hvmc:..</text></g><g><title>hvmc`hvmc::run::Net::comm02 (128 samples, 6.89%)</title><rect x="44.2949%" y="277" width="6.8891%" height="15" fill="rgb(241,131,45)" fg:x="823" fg:w="128"/><text x="44.5449%" y="287.50">hvmc`hvmc..</text></g><g><title>hvmc`hvmc::run::Net::comm22 (170 samples, 9.15%)</title><rect x="51.1841%" y="277" width="9.1496%" height="15" fill="rgb(249,31,29)" fg:x="951" fg:w="170"/><text x="51.4341%" y="287.50">hvmc`hvmc::ru..</text></g><g><title>hvmc`alloc::raw_vec::RawVec&lt;T,A&gt;::reserve_for_push (1 samples, 0.05%)</title><rect x="60.2799%" y="261" width="0.0538%" height="15" fill="rgb(225,111,53)" fg:x="1120" fg:w="1"/><text x="60.5299%" y="271.50"></text></g><g><title>hvmc`alloc::raw_vec::finish_grow (1 samples, 0.05%)</title><rect x="60.2799%" y="245" width="0.0538%" height="15" fill="rgb(238,160,17)" fg:x="1120" fg:w="1"/><text x="60.5299%" y="255.50"></text></g><g><title>libsystem_malloc.dylib`_realloc (1 samples, 0.05%)</title><rect x="60.2799%" y="229" width="0.0538%" height="15" fill="rgb(214,148,48)" fg:x="1120" fg:w="1"/><text x="60.5299%" y="239.50"></text></g><g><title>libsystem_malloc.dylib`_malloc_zone_realloc (1 samples, 0.05%)</title><rect x="60.2799%" y="213" width="0.0538%" height="15" fill="rgb(232,36,49)" fg:x="1120" fg:w="1"/><text x="60.5299%" y="223.50"></text></g><g><title>libsystem_malloc.dylib`szone_realloc (1 samples, 0.05%)</title><rect x="60.2799%" y="197" width="0.0538%" height="15" fill="rgb(209,103,24)" fg:x="1120" fg:w="1"/><text x="60.5299%" y="207.50"></text></g><g><title>libsystem_kernel.dylib`vm_copy (1 samples, 0.05%)</title><rect x="60.2799%" y="181" width="0.0538%" height="15" fill="rgb(229,88,8)" fg:x="1120" fg:w="1"/><text x="60.5299%" y="191.50"></text></g><g><title>libsystem_kernel.dylib`mach_msg2_trap (1 samples, 0.05%)</title><rect x="60.2799%" y="165" width="0.0538%" height="15" fill="rgb(213,181,19)" fg:x="1120" fg:w="1"/><text x="60.5299%" y="175.50"></text></g><g><title>hvmc`hvmc::run::Net::half_free (306 samples, 16.47%)</title><rect x="60.3337%" y="277" width="16.4693%" height="15" fill="rgb(254,191,54)" fg:x="1121" fg:w="306"/><text x="60.5837%" y="287.50">hvmc`hvmc::run::Net::half..</text></g><g><title>libsystem_kernel.dylib`__psynch_cvbroad (22 samples, 1.18%)</title><rect x="76.8568%" y="261" width="1.1841%" height="15" fill="rgb(241,83,37)" fg:x="1428" fg:w="22"/><text x="77.1068%" y="271.50"></text></g><g><title>libsystem_kernel.dylib`__psynch_cvwait (222 samples, 11.95%)</title><rect x="78.0409%" y="261" width="11.9483%" height="15" fill="rgb(233,36,39)" fg:x="1450" fg:w="222"/><text x="78.2909%" y="271.50">libsystem_kernel.d..</text></g><g><title>libsystem_pthread.dylib`_pthread_mutex_droplock (5 samples, 0.27%)</title><rect x="89.9892%" y="245" width="0.2691%" height="15" fill="rgb(226,3,54)" fg:x="1672" fg:w="5"/><text x="90.2392%" y="255.50"></text></g><g><title>libsystem_kernel.dylib`__psynch_mutexwait (110 samples, 5.92%)</title><rect x="90.2583%" y="229" width="5.9203%" height="15" fill="rgb(245,192,40)" fg:x="1677" fg:w="110"/><text x="90.5083%" y="239.50">libsyste..</text></g><g><title>libsystem_pthread.dylib`_pthread_cond_wait (116 samples, 6.24%)</title><rect x="89.9892%" y="261" width="6.2433%" height="15" fill="rgb(238,167,29)" fg:x="1672" fg:w="116"/><text x="90.2392%" y="271.50">libsyste..</text></g><g><title>libsystem_pthread.dylib`_pthread_mutex_firstfit_lock_slow (111 samples, 5.97%)</title><rect x="90.2583%" y="245" width="5.9742%" height="15" fill="rgb(232,182,51)" fg:x="1677" fg:w="111"/><text x="90.5083%" y="255.50">libsyste..</text></g><g><title>libsystem_pthread.dylib`DYLD-STUB$$__psynch_mutexwait (1 samples, 0.05%)</title><rect x="96.1787%" y="229" width="0.0538%" height="15" fill="rgb(231,60,39)" fg:x="1787" fg:w="1"/><text x="96.4287%" y="239.50"></text></g><g><title>hvmc`std::sync::barrier::Barrier::wait (371 samples, 19.97%)</title><rect x="76.8030%" y="277" width="19.9677%" height="15" fill="rgb(208,69,12)" fg:x="1427" fg:w="371"/><text x="77.0530%" y="287.50">hvmc`std::sync::barrier::Barrie..</text></g><g><title>libsystem_pthread.dylib`_pthread_mutex_firstfit_lock_slow (10 samples, 0.54%)</title><rect x="96.2325%" y="261" width="0.5382%" height="15" fill="rgb(235,93,37)" fg:x="1788" fg:w="10"/><text x="96.4825%" y="271.50"></text></g><g><title>libsystem_kernel.dylib`__psynch_mutexwait (10 samples, 0.54%)</title><rect x="96.2325%" y="245" width="0.5382%" height="15" fill="rgb(213,116,39)" fg:x="1788" fg:w="10"/><text x="96.4825%" y="255.50"></text></g><g><title>libsystem_malloc.dylib`free_medium (1 samples, 0.05%)</title><rect x="96.7707%" y="277" width="0.0538%" height="15" fill="rgb(222,207,29)" fg:x="1798" fg:w="1"/><text x="97.0207%" y="287.50"></text></g><g><title>libsystem_kernel.dylib`madvise (1 samples, 0.05%)</title><rect x="96.7707%" y="261" width="0.0538%" height="15" fill="rgb(206,96,30)" fg:x="1798" fg:w="1"/><text x="97.0207%" y="271.50"></text></g><g><title>libsystem_pthread.dylib`_pthread_mutex_firstfit_unlock_slow (53 samples, 2.85%)</title><rect x="96.8245%" y="277" width="2.8525%" height="15" fill="rgb(218,138,4)" fg:x="1799" fg:w="53"/><text x="97.0745%" y="287.50">li..</text></g><g><title>libsystem_kernel.dylib`__psynch_mutexdrop (53 samples, 2.85%)</title><rect x="96.8245%" y="261" width="2.8525%" height="15" fill="rgb(250,191,14)" fg:x="1799" fg:w="53"/><text x="97.0745%" y="271.50">li..</text></g><g><title>hvmc`std::sys::unix::thread::Thread::new::thread_start (1,838 samples, 98.92%)</title><rect x="0.8073%" y="325" width="98.9236%" height="15" fill="rgb(239,60,40)" fg:x="15" fg:w="1838"/><text x="1.0573%" y="335.50">hvmc`std::sys::unix::thread::Thread::new::thread_start</text></g><g><title>hvmc`core::ops::function::FnOnce::call_once{{vtable.shim}} (1,838 samples, 98.92%)</title><rect x="0.8073%" y="309" width="98.9236%" height="15" fill="rgb(206,27,48)" fg:x="15" fg:w="1838"/><text x="1.0573%" y="319.50">hvmc`core::ops::function::FnOnce::call_once{{vtable.shim}}</text></g><g><title>hvmc`std::sys_common::backtrace::__rust_begin_short_backtrace (1,745 samples, 93.92%)</title><rect x="5.8127%" y="293" width="93.9182%" height="15" fill="rgb(225,35,8)" fg:x="108" fg:w="1745"/><text x="6.0627%" y="303.50">hvmc`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>libsystem_pthread.dylib`pthread_cond_wait (1 samples, 0.05%)</title><rect x="99.6771%" y="277" width="0.0538%" height="15" fill="rgb(250,213,24)" fg:x="1852" fg:w="1"/><text x="99.9271%" y="287.50"></text></g><g><title>libsystem_kernel.dylib`__munmap (4 samples, 0.22%)</title><rect x="99.7309%" y="325" width="0.2153%" height="15" fill="rgb(247,123,22)" fg:x="1853" fg:w="4"/><text x="99.9809%" y="335.50"></text></g><g><title>all (1,858 samples, 100%)</title><rect x="0.0000%" y="373" width="100.0000%" height="15" fill="rgb(231,138,38)" fg:x="0" fg:w="1858"/><text x="0.2500%" y="383.50"></text></g><g><title>libsystem_pthread.dylib`thread_start (1,843 samples, 99.19%)</title><rect x="0.8073%" y="357" width="99.1927%" height="15" fill="rgb(231,145,46)" fg:x="15" fg:w="1843"/><text x="1.0573%" y="367.50">libsystem_pthread.dylib`thread_start</text></g><g><title>libsystem_pthread.dylib`_pthread_start (1,843 samples, 99.19%)</title><rect x="0.8073%" y="341" width="99.1927%" height="15" fill="rgb(251,118,11)" fg:x="15" fg:w="1843"/><text x="1.0573%" y="351.50">libsystem_pthread.dylib`_pthread_start</text></g><g><title>libsystem_pthread.dylib`_pthread_exit (1 samples, 0.05%)</title><rect x="99.9462%" y="325" width="0.0538%" height="15" fill="rgb(217,147,25)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="335.50"></text></g><g><title>libsystem_pthread.dylib`_pthread_tsd_cleanup (1 samples, 0.05%)</title><rect x="99.9462%" y="309" width="0.0538%" height="15" fill="rgb(247,81,37)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="319.50"></text></g><g><title>dyld`dyld4::RuntimeState::_finalizeListTLV (1 samples, 0.05%)</title><rect x="99.9462%" y="293" width="0.0538%" height="15" fill="rgb(209,12,38)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="303.50"></text></g><g><title>dyld`invocation function for block in dyld4::RuntimeState::_finalizeListTLV (1 samples, 0.05%)</title><rect x="99.9462%" y="277" width="0.0538%" height="15" fill="rgb(227,1,9)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="287.50"></text></g><g><title>hvmc`std::sys::unix::thread_local_dtor::register_dtor::run_dtors (1 samples, 0.05%)</title><rect x="99.9462%" y="261" width="0.0538%" height="15" fill="rgb(248,47,43)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="271.50"></text></g><g><title>hvmc`alloc::sync::Arc&lt;T,A&gt;::drop_slow (1 samples, 0.05%)</title><rect x="99.9462%" y="245" width="0.0538%" height="15" fill="rgb(221,10,30)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="255.50"></text></g><g><title>libobjc.A.dylib`_objc_msgSend_uncached (1 samples, 0.05%)</title><rect x="99.9462%" y="229" width="0.0538%" height="15" fill="rgb(210,229,1)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="239.50"></text></g><g><title>libobjc.A.dylib`lookUpImpOrForward (1 samples, 0.05%)</title><rect x="99.9462%" y="213" width="0.0538%" height="15" fill="rgb(222,148,37)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="223.50"></text></g><g><title>libobjc.A.dylib`initializeAndMaybeRelock(objc_class*, objc_object*, locker_mixin&lt;lockdebug::lock_mixin (1 samples, 0.05%)</title><rect x="99.9462%" y="197" width="0.0538%" height="15" fill="rgb(234,67,33)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="207.50"></text></g><g><title>libobjc.A.dylib`initializeNonMetaClass (1 samples, 0.05%)</title><rect x="99.9462%" y="181" width="0.0538%" height="15" fill="rgb(247,98,35)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="191.50"></text></g><g><title>libobjc.A.dylib`initializeNonMetaClass (1 samples, 0.05%)</title><rect x="99.9462%" y="165" width="0.0538%" height="15" fill="rgb(247,138,52)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="175.50"></text></g><g><title>libobjc.A.dylib`_objc_sync_enter_kind (1 samples, 0.05%)</title><rect x="99.9462%" y="149" width="0.0538%" height="15" fill="rgb(213,79,30)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="159.50"></text></g><g><title>libobjc.A.dylib`id2data(objc_object*, SyncKind, usage) (1 samples, 0.05%)</title><rect x="99.9462%" y="133" width="0.0538%" height="15" fill="rgb(246,177,23)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="143.50"></text></g><g><title>libsystem_malloc.dylib`posix_memalign (1 samples, 0.05%)</title><rect x="99.9462%" y="117" width="0.0538%" height="15" fill="rgb(230,62,27)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="127.50"></text></g><g><title>libsystem_malloc.dylib`_malloc_zone_memalign (1 samples, 0.05%)</title><rect x="99.9462%" y="101" width="0.0538%" height="15" fill="rgb(216,154,8)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`tiny_memalign (1 samples, 0.05%)</title><rect x="99.9462%" y="85" width="0.0538%" height="15" fill="rgb(244,35,45)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="95.50"></text></g><g><title>libsystem_malloc.dylib`tiny_malloc_should_clear (1 samples, 0.05%)</title><rect x="99.9462%" y="69" width="0.0538%" height="15" fill="rgb(251,115,12)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`mvm_allocate_pages (1 samples, 0.05%)</title><rect x="99.9462%" y="53" width="0.0538%" height="15" fill="rgb(240,54,50)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="63.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_map_trap (1 samples, 0.05%)</title><rect x="99.9462%" y="37" width="0.0538%" height="15" fill="rgb(233,84,52)" fg:x="1857" fg:w="1"/><text x="100.1962%" y="47.50"></text></g></svg></svg>