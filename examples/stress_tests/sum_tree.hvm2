data Tree = (Node x y) | (Leaf v)

add = λa λb (+ a b)

gen = λn match n {
  0   : (Leaf 1)
  1+p : (Node (gen p) (gen p))
}

sum = λt
  let case_node = λa λb (add (sum a) (sum b))
  let case_leaf = λv v
  (t case_node case_leaf)

main = (sum (gen 26))
