// closed net for the Church Nat 2
@c2_a = ([(b a) (a R)] (b R))

// also 2, but with a different label
@c2_b = ({2 (b a) (a R)} (b R))

// applies 2 to 2 to exponentiate
@main
  = ret
  & @c2_a ~ (@c2_b ret)
